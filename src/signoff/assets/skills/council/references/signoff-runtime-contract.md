# Signoff runtime contract

During a Signoff mission, `COUNCIL.json` is the canonical output. Preserve the exact artifact hashes. `./signoff lock` rejects fake quorum, duplicate contexts, unresolved verdict-split conflicts, and vote-based arbitration.
