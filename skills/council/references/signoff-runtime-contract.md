# Signoff runtime contract

During a Signoff mission, `COUNCIL.json` is the canonical output. Preserve the exact artifact hashes and four-topic claim registry. `./signoff lock` rejects fake quorum, duplicate contexts, different questions, unresolved SUPPORT/OPPOSE conflicts, and vote-based arbitration.
