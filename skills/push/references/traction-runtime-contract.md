# Traction runtime contract

During a Traction run, `PUSH.json` is the canonical output. Preserve the exact artifact hashes. `./traction lock` rejects fake quorum, duplicate contexts, unresolved verdict-split conflicts, and vote-based arbitration.
