Generated ownership decision for Todo 6.

Verdict: current dirty/stale generated bundle state is Todo 7-owned, not Todo 6-owned.

Paths:
- `src/signoff/web_dist/`: Todo 7-owned.
- `apps/web/dist/`: Todo 7-owned.

Why:
- Todo 6 scope is source UI behavior only: `apps/web/src/components/ArtifactPanel.tsx`, `apps/web/src/pages/Dashboard.tsx`, and `apps/web/src/styles.css`.
- `.omo/start-work/notepad.md` states: "Generated bundle changes must remain unclaimed by Todos 1-6 until Todo 7 rebuilds packaged UI through `npm run build:web`."
- `.omo/plans/signoff-ui-flow.md` Todo 7 is the first task that owns rebuilding packaged UI through `npm run build:web`.
- Current status still shows generated bundle dirt under `src/signoff/web_dist/` and generated files under `apps/web/dist/`; the Todo 6 QA rerun did not run `npm run build:web` and did not hand-edit generated assets.

Evidence:
- `.omo/evidence/task-6-signoff-ui-flow-generated-status.txt`
